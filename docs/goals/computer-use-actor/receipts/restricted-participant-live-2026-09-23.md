# Restricted account participant: browser and cancellation proof

This receipt qualifies one internal Linux x64 account participant over an
already-owned browser executor. It does not release a managed local runtime.
The [contract](restricted-participant-contract.md) describes the finite request,
memory, output and accounting boundaries.

Source `c0b8d4c1b12b7be9c8d87107fa718dac5b86959c` used the unchanged qualified
Codex CLI 0.154.0 launcher, ChatGPT account authentication, `gpt-6-astra`, low
reasoning effort and the restricted tool policy. The production package and
Observer were built from that source. No API-key fallback was used.

## Completed study and automatic analysis

The participant saw the maintained synthetic note page, served by a fixed
test-only HTTP server on the container's loopback interface. It was asked to
enter `Café 日本語 🙂`, save once and assess the visible result. The host prepared
the page before handing over the executor; those setup inputs are recorded
separately from participant actions.

Two real model requests produced three acknowledged participant inputs: click
the draft, type the requested text, and click Save. Independent visual review
of the actual captures confirmed the exact displayed text and the change from
“Saved 0” to “Saved 1.” This establishes the visible result; reload persistence
was not tested. The participant's `reached` outcome remains a participant report
in the bundle, separately from that review.

The normal `runLab` producer wrote the recording and participant feedback.
After confirmed provider cleanup and positive owned-container absence, its
ordinary completion boundary dispatched a separately configured account
analyst. Analysis completed with nine evidence items, both captures and zero
findings. It did not invent friction for the successful control task.

| Request scope | Input tokens | Output tokens | Dollar estimate |
| --- | ---: | ---: | --- |
| Participant, two requests | 7,992 | 181 | Unknown |
| Automatic analyst | 7,284 | 523 | Unknown |

The run and Observer preserve the account execution profile and null dollar
estimates. Actual desktop and 390px Observer inspection checked rendered
captures, participant feedback, the unknown-cost label, report availability
and the recorded-statement link to its exact frame/event. Neither width had
horizontal page overflow or broken captures.

Retained normal run ID: `account-note-proof`. Proof receipt SHA256:
`c01f1d9d9b30b62346b1bb79ebfef6175a5bf6e772b80a9e191c8637143ea02f`.

## Cancellation after actual dispatch

A separate fresh study used passive instrumentation of the real native
launcher. It observed one scoped `turn/started` notification and then aborted
the study. Selected output-related protocol notifications arrived afterward;
none became accepted participant narration or browser input.

The ordinary bundle records a harness cancellation, zero participant actions,
zero accepted output items and one dispatched request with confirmed cleanup.
That request did not report token usage; absent counts remain absent. They are
not recorded as zero tokens or zero dollars. Both owned native Codex children
closed, and the owned browser container was positively absent.

The automatic completion hook ran, but analysis returned
`AUTOMATIC_ANALYSIS_ACTOR_CANCELLED` before invoking the analyst provider.
Instrumentation recorded zero analyst invocations and zero analyst children.

Retained cancellation run ID: `account-cancellation-proof`. Proof receipt SHA256:
`14f7a0b210fe6f8950ebe4747aa9f7b26e57d81cde613ded36151d87e6353b1d`.

## Scope and checks

Both studies used the accepted headed browser package in ordinary constrained
Docker: read-only root, 1,536 MiB with no additional swap allowance, 256 tasks,
no external network, no host mounts or devices, and Chromium's sandbox enabled.
The fixture's loopback server is proof scaffolding. Model/account access remained
in the host's existing restricted launcher. This does not qualify VM isolation,
AF_VSOCK, host-app networking or a general Docker runtime mode.

Full source checks passed 3,669 core tests, 364 Observer tests and 86 TUI tests,
plus typechecks, builds, CLI checks, documentation and public-surface scanning.
Independent source review accepted the request lifecycle, accounting and
reader changes before the actual calls.

Earlier attempts remain retained: a proof-image preparation failure occurred
before model dispatch; an initial successful file-page study exposed a mismatch
with its declared localhost subject. The final HTTP-page study above corrected
that setup. The normal producer still describes caller-provisioned local-app
source as unpinned; the separate proof receipt binds the test image, not a new
product provenance feature.

This is one normal study and one actual cancellation, not reliability at cohort
scale. Managed installation, Mac account qualification, multiple participants,
parallel account calls, media and the complete local runtime remain open.
No npm release accompanies this internal integration.
