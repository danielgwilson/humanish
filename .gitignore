# dependencies
node_modules/
.pnp
.pnp.*

# build and caches
dist/
build/
coverage/
.turbo/
.cache/
.firecrawl/
*.tsbuildinfo

# proof/runtime artifacts
.humanish/
# legacy runtime dir from before the humanish rename; old checkouts still carry it
.homun/
artifacts/proof/
proof/
.e2b/
*.tgz

# env and secrets
.env*
!.env.example
.npmrc
!.npmrc.example

# local worktrees if someone creates them inside the repo by mistake
.worktrees/
worktrees/

# OS/editor noise
.DS_Store
*.log

# python fixture caches
__pycache__/
*.pyc
